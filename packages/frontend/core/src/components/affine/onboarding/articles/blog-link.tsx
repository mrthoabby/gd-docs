import { link } from './blocks.css';

// [SELFHOST PATCH] Link externo a affine.pro/blog eliminado
export const BlogLink = () => {
  return (
    <span className={link}>Check our documentation</span>
  );
};
